# Salvaged audit output (workflow stopped mid-refute)

Raw findings from the 4 audit lenses: 53
Refuter verdicts completed: 27 (refuted=11, upheld=16)

## Findings (UNVERIFIED — refuter phase was cut short)

### [critical] scripts/run_stages.sh:242 — --no-cvat leaves cvat3d in STEPS: --clean-slate --no-cvat now purges the CVAT server
- **claim:** The diff added `cvat3d` to ALL_STEPS (line 216) and to the clean-slate purge condition (line 328), but the `--no-cvat` filter at line 242 still strips only the exact word `cvat`. So `--no-cvat` leaves `cvat3d` in STEPS, which (a) satisfies the purge branch `*" cvat "* || *" cvat3d "*` and (b) runs `cvat_setup_3d.py --replace`. Before this diff ALL_STEPS had no cvat3d, so `--no-cvat` emptied the CVAT step set and the purge branch was false. Line 74's `# Steps:` header was also not updated for cvat3d.
- **failure:** Simulated: `scripts/run_stages.sh --clean-slate --no-cvat` (documented at line 68 as "full chain, no publish") printed `steps: 0 1 3 4 5 6 7 8 eval viz cvat3d` and then invoked `ARGV: [scripts/cvat_purge.py] [--host] [http://localhost:8081] [--user] [mt] [--yes]`. cvat_purge.py's own docstring: "Delete EVERY task and project on the CVAT server" including "the 3D tasks and every cuboid in them — **NOT regenerable**… Deleting them is a one-way door." The operator's explicit instruction not to touch the review server irreversibly destroys it, then republishes 3D over the wreckage.

### [critical] pipeline/stage3b_track2d/track2d.py:1 — No upper size gate on injection: leaked full-frame mask becomes a proposal
- **claim:** The injection gates recorded in trial_3b/run_manifest.json are lower bounds only (min_recovered_mask_px=16, min_recovered_box_side_px=4.0). Nothing bounds a propagated mask from above, and refine_area_band [0.6,1.4] guards only the refine path (refine_matched_boxes=false here). When SAM's propagated mask leaks off the object, the tight box becomes the whole image and is injected as a detection-shaped proposal with full class/score provenance. (Anchored at line 1 on purpose: this file is under concurrent edit by another agent, so I did not read it and cannot cite a stable line. Evidence is entirely from the artifacts and the config surface the manifest records.)
- **failure:** scene-0061 CAM_FRONT_LEFT track 0 is a truck leaving frame right: YOLO box [801.5,294.1,1594.1,886.6] @0.7057 at keyframe 20ce9ebe. At the next keyframe (25cd4f36) the propagated mask covers the whole image and 3b injects boxes_xyxy_px=[0.0,0.0,1600.0,900.0], class 'a truck', score 0.49401, hops 1 -- 3.07x the parent area, 100% of the frame. At the following keyframe (a5a93490) it injects [0.0,0.0,990.0,900.0] @0.34581, hops 2. Stage 4 (trial_3b_stage4/scenes/scene-0061/masks.jsonl, keyframe e0845f5322254dafadbbed75aaa07969) segments the first to n_mask_px=1069272 (74% of the frame), marks kept:true, and its angular_footprint az[0.42,1.54] el[-0.32,0.36] (64 deg x 39 deg) SUPPRESSES a legitimate CAM_FRONT recovered truck at suppression_ioa 1.0. Stage 5 then paints every LiDAR return in that 64x39 deg cone as one vehicle.truck instance at score 0.494, and stages 6-8 emit a giant false 3D box. Distribution over the 71 recovered boxes: 1 at 100% of frame, 1 at 62%, 2 exceed 2x the parent detection area (3.80x, 3.07x), 28 of 71 are below 0.4x the parent.

### [major] pipeline/stage4_masks/masks.py:1063 — F1's basename guard refuses the HF cache's real path, breaking the C26 smoke gate
- **claim:** The check is `os.path.basename(explicit) != _CHECKPOINT_FILENAME` on the raw string, but the hub cache stores `snapshots/<rev>/sam3.1_multiplex.pt` as a SYMLINK to `blobs/<sha256>`. Any caller that resolves the symlink before handing the path over is now refused — and scripts/smoke_sam31.py:106 (`_resolve_checkpoint`) does exactly that: `return os.path.realpath(override)`. Its result is passed straight into `MaskConfig(checkpoint_path=checkpoint)` at smoke_sam31.py:610, so the adapter sees a basename of 64 hex chars. Note `_resolve_checkpoint` itself calls `os.path.realpath(explicit)` at line 1074 only AFTER the check, so a raw snapshot path survives while its own realpath does not.
- **failure:** Verified on this machine: `/home/mt/dhakascenes/cache/huggingface/hub/models--facebook--sam3.1/snapshots/daa6319.../sam3.1_multiplex.pt` is a symlink to `../../blobs/0567debeec80...`. I built a Sam31MultiplexAdapter with each string: the snapshot path resolves fine; the realpath'd blob path raises `ModelUnavailable: checkpoint_path=... is not sam3.1_multiplex.pt`. So `python3 scripts/smoke_sam31.py --mode adapter --checkpoint <that snapshot path>` — the documented C26 GO/NO-GO invocation, and the only place the file exists on this box — now dies in `build()` before the predictor is constructed, with an error telling the operator the path is 'the mobile_sam channel'.

### [major] scripts/run_pilot.py:137 — run_pilot pairs track.py's DINOv3 default id with DINOv2's sha; re-ID silently disabled
- **claim:** The same uncommitted change set moved the re-ID default to DINOv3 in run_stages.sh (new REID_PINNED table) and in `pipeline/stage7_track/track.py:202` (`reid_model_id: str = "facebook/dinov3-vits16-pretrain-lvd1689m"`), but run_pilot.py:86 still hardcodes `REID_REVISION = "ed25f3a31f01632728cabb09d1542f84ab7b0056"` (DINOv2-small's sha) and line 137 passes `--reid-revision` with **no** `--reid-model-id`. A sha belongs to one repo — the exact hazard the REID_PINNED table was added to run_stages.sh to prevent. This contradicts run_pilot.py:74-76 ("identical defaults to scripts/run_stages.sh … so the two drivers cannot silently diverge on which model a stage loads").
- **failure:** `python -m scripts.run_pilot` (default chain) reaches stage 7, which requests facebook/dinov3-vits16-pretrain-lvd1689m at revision ed25f3a3… → hub RevisionNotFound → ModelUnavailable. track.py:1745-1753 catches it and, since `require_appearance` is off, re-runs IoU-only: the stage exits 0 with a clean marker while appearance re-ID is silently off. run_pilot's aggregate manifest (line 348) nonetheless records `"reid_embedding": {"revision": "ed25f3a3…"}` as the pin that ran, and the pilot's tracking numbers are produced without the re-ID stage the record claims.

### [major] scripts/run_stages.sh:475 — stage3_dir_for_4 checks mtime but not scene coverage; a subset 3b tree hijacks a full run
- **claim:** `stage3_dir_for_4` gates only on `marker_state != none` and an mtime `-nt` comparison. It never checks that stage3b_track2d/scenes covers the same scene set as stage3_proposals/scenes. masks.py:1830-1831 enumerates scenes by `os.listdir(os.path.join(stage3_dir, "scenes"))`, so whatever the chosen tree contains *is* the run's scope, silently.
- **failure:** The plan's own ordered execution prescribes "one-scene trial → full run". Operator runs `scripts/run_stages.sh 3b --scenes scene-0061` (3b tree now holds 1 scene, marker clean, manifest newest), then resumes the full pipeline with `scripts/run_stages.sh 4 5 6 7 8 eval`. stage3_dir_for_4 returns stage3b_track2d, masks.py finds one scene under it, and Stage 4 through eval run over 1/10 of the data with rc 0 and clean markers everywhere — no warning, no scene-count mismatch reported. `scripts/save_run_results.py:106` then writes `"scenes"` from the **Stage 3** manifest (all ten) into Results/<label>/run_config.json alongside metrics measured on one scene.

### [major] scripts/run_pilot.py:254 — C16 pre-scan arms --accept-degraded-upstream from a stale stage3b marker
- **claim:** CHAIN gained "3b", and the pre-existing pre-scan at lines 254-258 reads the marker of every CHAIN step NOT in `--steps`. Since DEFAULT_STEPS deliberately excludes "3b", every default run now inspects stage3b_track2d — a tree that run is neither producing nor consuming (line 129 only adds `--stage3-dir` when "3b" IS selected). A leftover degraded 3b tree therefore arms the C16 opt-in for the whole chain.
- **failure:** Reproduced with the exact loop against a work_root holding only a leftover `_SUCCESS.degraded` in stage3b_track2d (causes: "3 propagation windows failed") from an earlier A/B cell, stage1 clean: `accept_degraded = True`. Every stage of the baseline run is then launched with `--accept-degraded-upstream` and records `upstream.accepted_degraded_upstream: true` in its own manifest — a false provenance claim, since no upstream this run read was degraded. It also disarms the C16 refusal for stages that genuinely degrade later, and forces `return EXIT_FLAGGED` (rc 1) on line 369 for a run in which nothing was flagged.

### [major] scripts/export_cvat_coco.py:124 — source/track_id/hops are not declared in cvat_setup label_spec, so CVAT drops them
- **claim:** The export writes `attributes.source`, `attributes.track_id` and `attributes.hops` on both annotation dicts (lines 124-126 and 143-145), but `scripts/cvat_setup.py:40-63` `label_spec()` declares only `score` and `suppressed` on each label, and the project's labels are created once (line 118-122) and never updated for an existing project. cvat_setup.py's own docstring states the mechanism: labels carry "`score` and `suppressed` attributes **so the COCO `attributes` survive import**" — undeclared attributes have no declaration to survive into.
- **failure:** Run `scripts/run_stages.sh 0 1 3 3b 4 5 6 7 8 eval cvat`. instances.json correctly carries `"source": "recovered", "track_id": 41, "hops": 2` for every injected box, but cvat_setup.py imports it into a project whose labels declare only score/suppressed, so the reviewer opening the task sees a recovered box that is indistinguishable from a detected one. export_cvat_coco.py's own docstring claim — "a box no detector ever saw is visible AS a recovered box in CVAT" — does not hold, and the plan's CVAT-visibility requirement is unmet end to end.

### [major] scripts/run_stages.sh:364 — Resuming at stage 4 after a DEGRADED Stage 3b aborts; pre-scan only reads stage0/stage1
- **claim:** The C16 pre-scan at line 364 arms DEGRADED_SEEN from `stage1_ingestion` and `stage0_data_probe` only. `stage3_dir_for_4` (line 475) returns the 3b tree when its marker is `degraded` (proven by simulation), so a resume run points Stage 4 at a degraded upstream while `acc` leaves ACC empty. `pipeline/common/manifest.py:288` (`if marker.degraded and not accept_degraded: raise UpstreamRefusal`) then refuses. Stage 3b makes DEGRADED a *designed* normal outcome — the plan's B4.6: "Any provider exception in one window → identity fallback … counted, exit DEGRADED."
- **failure:** Simulated argv for `scripts/run_stages.sh 4` with a fresh `_SUCCESS.degraded` in stage3b_track2d: `[--stage3-dir] [<wr>/stage3b_track2d]` with no `--accept-degraded-upstream`. masks.py raises UpstreamRefusal → rc 2 → run_step marks REFUSED and aborts the chain. So `scripts/run_stages.sh 3 3b` (finishes DEGRADED, ~45-70 min of GPU work) followed by the documented resume `scripts/run_stages.sh 4 5 6 7 8 eval` dies immediately, and no env var in the script can arm the flag.

### [major] scripts/run_pilot.py:129 — Resume after a 3b chain silently drops --stage3-dir; disagrees with run_stages.sh
- **claim:** `stage_cmd` adds `--stage3-dir <work_root>/stage3b_track2d` only when "3b" is in *this invocation's* `--steps`. There is no marker or freshness check on the 3b tree at all, so the flag's presence tracks the command line rather than the disk. run_stages.sh's `stage3_dir_for_4` decides the opposite way (from the disk) on identical state.
- **failure:** Verified by calling stage_cmd directly: `stage_cmd("4", ["s1"], False, WR, ["4"])` returns `['-m','pipeline.stage4_masks.masks','--scenes','s1','--model-id','facebook/sam3','--revision','3c879f39…']` — no `--stage3-dir`. So `python -m scripts.run_pilot --steps 3 3b 4 5 6 7 8 9` that dies at stage 5, resumed with the documented `--steps 5 6 7 8 9`, is fine, but the equally documented resume `--steps 4 5 6 7 8 9` silently re-runs Stage 4 against `stage3_proposals` (baseline YOLO boxes), succeeding with rc 0 and a clean marker, discarding every recovered box the 3b pass produced — while `scripts/run_stages.sh 4` on the very same work_root would have used stage3b_track2d.

### [major] pipeline/stage4_masks/masks.py:1930 — Stage 3b writes clean _SUCCESS over a degraded Stage 3, erasing the C16 flag
- **claim:** Stage 3b consumed a DEGRADED Stage 3 tree (its own manifest records upstream.degraded=true with all 10 causes and accepted_degraded_upstream=true) but wrote a plain _SUCCESS marker, not _SUCCESS.degraded. masks.py:1930-1931 sources upstream.degraded/degraded_causes from the IMMEDIATE upstream marker via require_upstream, so inserting 3b converts a flagged chain into an unflagged one. The 3b tree does not even satisfy stage 3's own degradation predicate: proposals.py:1952 flags any scene with n_images_with_zero_proposals>0, and the 3b tree still has 85 zero-proposal images out of 234 rows in scene-0061 (3b filled only 15 of the original 100).
- **failure:** Compare the two stage-4 manifests on disk. s4_det_1/run_manifest.json (fed from /home/mt/dhakascenes/work/stage3_proposals, which carries _SUCCESS.degraded) records upstream.degraded=true and all 10 cause strings. trial_3b_stage4/run_manifest.json (fed from trial_3b, which carries plain _SUCCESS) records upstream.degraded=false and degraded_causes=[] -- for detections that came from exactly the same degraded YOLO run. Beyond the false record: in any later invocation, e.g. `scripts/run_stages.sh 4 5 6 7 8 eval` after a 3b pass, run_stages.sh:364-366 arms DEGRADED_SEEN only from stage0/stage1 markers, so acc() passes no flag; require_upstream (manifest.py:293) sees 3b's clean marker and does not demand --accept-degraded-upstream. C16's explicit-opt-in gate is bypassed for stages 4-8 and the 10 causes disappear from every manifest downstream of 3b.

### [major] scripts/run_stages.sh:475 — stage3_dir_for_4 adopts a PARTIAL 3b tree, silently narrowing the whole chain
- **claim:** stage3_dir_for_4 selects the 3b tree on two conditions only -- a marker exists and its run_manifest.json is newer than stage 3's. It guards against a stale tree but not against a tree built with --scenes over a subset. masks.py:1832 enumerates scenes with os.listdir(<stage3_dir>/scenes) and run_stages.sh passes --scenes to stage 4 only when the user typed it (SCENE_ARGS is empty otherwise, line 248), so the 3b tree's scene set silently becomes the run's scene set. masks.py's 'requested scene(s) not present' refusal at :1836 only fires when --scenes is given, so nothing refuses. run_pilot.py:129 keys the same decision on this run's step selection instead, so the two runners disagree about the same intent.
- **failure:** Exactly the tree this audit was given: trial_3b/ holds one scene (scene-0061), carries a clean _SUCCESS, and its run_manifest.json (Aug 19) is newer than stage3_proposals/run_manifest.json (Aug 14). Run a full 10-scene baseline, then `scripts/run_stages.sh 3b --scenes scene-0061` for a trial, then `scripts/run_stages.sh 4 5 6 7 8 eval` with no --scenes. stage3_dir_for_4 returns the 3b dir, masks.py lists one scene, masks 39 keyframes instead of 404, exits 0 and writes a clean _SUCCESS over stage4_masks/. Stages 5-8 then run over that tree and eval writes detect2d/detect3d/paint metrics computed on 1 of 10 scenes. Every step reports success; the only trace is the counts buried in the manifests, and Results/<label>/run_config.json records track2d.consumed_by_stage4=true as if the full arm had run.

### [major] docs/DECISIONS.md:930 — C26 'Recorded in' cites docs/conformance.yaml, which has no C26 content
- **claim:** C26's Recorded-in list ends with `docs/conformance.yaml.` (DECISIONS.md:930). docs/conformance.yaml is UNMODIFIED in this change-set (`git status` does not list it) and `grep -n 'C26\|sam3\|sam31\|SAM 3' docs/conformance.yaml` returns zero hits. Not one row covers the new sam31_multiplex provider, the exact-match+refusal routing, the six compensations, or the smoke measurements. The rendered docs/CONFORMANCE.md (untracked, header 'Rendered 2026-08-19 14:19 at commit 4614a9c. 275 rows.') was regenerated from that unchanged yaml, so it too has no C26 row while carrying today's date.
- **failure:** A reviewer auditing C26 per BUILD_PROMPT §4 runs scripts/check_conformance.py and greps the ledger for the new provider. It returns nothing. Because C26 asserts the decision IS recorded there, the reviewer concludes the conformance ledger is complete and that no row is owed — the one selectable provider whose masks nothing has ever validated end-to-end passes the audit surface by being invisible to it.

### [major] docs/DECISIONS.md:932 — C26 Gate (2) names an A/B that never runs sam3.1 as mask_2d in any cell
- **claim:** Gate (2) reads 'Promotion to default requires the four-cell A/B (scripts/run_matrix_track2d.sh)'. All four cells in that script (scripts/run_matrix_track2d.sh:50-55) leave mask_2d on facebook/sam3; only cell 4 changes anything SAM 3.1-related, and it changes `TRACK2D_MODEL_ID` — Stage 3b's tracker — with its own note stating 'mask_2d stays on facebook/sam3'. The experiment named as the gate for promoting sam31_multiplex to the mask_2d default never once produces a Stage 4 mask with sam31_multiplex.
- **failure:** A human executes `scripts/run_matrix_track2d.sh`, sees cell 4 (yolo11x_3b_sam31) beat the others in compare_runs, and — following Gate (2) verbatim — flips the mask_2d default to facebook/sam3.1. The evidence they acted on measured Stage 3b box recovery, not Stage 4 segmentation; every keyframe mask in all four cells came from facebook/sam3. The default flips on a comparison that never tested it.

### [major] docs/DECISIONS.md:917 — Manifest sha256 claim is true only for the non-default sam31_multiplex
- **claim:** C26 states 'The stage-4 manifest now records the resolved provider and the checkpoint sha256 (the null-provider gap in every prior Results/run_config.json)'. In masks.py:1937-1941 the manifest fills sha256 from `getattr(adapter, "checkpoint_sha256", None) or spec.sha256`. Only Sam31MultiplexAdapter defines `checkpoint_sha256` (masks.py:887-889); MobileSamAdapter and TransformersSamAdapter do not, and run() builds CheckpointSpec at masks.py:1797-1803 without a sha256 (model_interfaces.py:182 defaults it to None). The provider half of the claim is true for all providers; the sha256 half is true only for sam31_multiplex, which C26 itself keeps non-default.
- **failure:** Someone runs Stage 4 on the standing default (facebook/sam3), saves it with save_run_results, and opens Results/<tag>/run_config.json expecting the gap C26 says is closed. mask_2d.sha256 is still `null` — identical to the four existing runs under Results/ (yolo11x_dinov2, yolo11x_dinov3, yolov8x-oiv7_dinov2, yolov8x-oiv7_dinov3, all `"sha256": null`). The run is quotable against a repo name, not bytes, while the ledger records the defect as fixed.

### [major] requirements-lock.txt:79 — Re-frozen lock has a dead /tmp file:// URL, so it will not install
- **claim:** The lock re-frozen for C26 (header line 1: 'Frozen 2026-08-19 after the C26 GPU smoke passed ... closes the re-freeze half of C19 gate (1) and C26 gate (1)') now carries `pyzed @ file:///tmp/selfgz389546928890/pyzed-5.4-cp310-cp310-linux_x86_64.whl`. That directory does not exist (`ls /tmp/selfgz389546928890` → No such file or directory). pyzed is also declared in none of requirements.txt, requirements-devkit.txt or requirements-torch.txt, contradicting requirements.txt's own header rule ('There is no fourth file and no manual `pip install <name>` step'). Three other hand-installed top-level packages entered the lock the same way (cvat_sdk, clip, Cython) — none was present in HEAD's lock.
- **failure:** Anyone reproducing the environment runs `pip install -r requirements-lock.txt` on this or another box. pip fails at line 79 with an unresolvable file:// URL before installing torch, sam3, or anything else. The file the ledger names as the closed-gate artifact for both C19 gate (1) and C26 gate (1) cannot rebuild the environment it claims to pin.

### [major] docs/DECISIONS.md:898 — C26's 'six compensations' omits one and renumbers the rest against masks.py
- **claim:** C26 enumerates exactly six compensations and numbers them (1) use_fa3, (2) hotstart_delay, (3) suppress_unmatched_only_within_hotstart, (4) start_session bypass, (5) cached_frame_outputs seed, (6) unload autocast. masks.py numbers its own six differently: `C26 compensation 1` at :945 (use_fa3), `2` at :962 (BOTH hotstart knobs together), `3` at :1339 (start_session bypass), `4` at :1369 (cached_frame_outputs seed), `5` at :1412 — 'one corner-pair point prompt per object' — and `6` at :983 (unload). The corner-pair box encoding is a seventh compensation the ledger never lists, and masks.py:1414-1420 records why it matters: the `bounding_boxes=` spelling 'routes to the semantic exemplar path, which opens with `self.reset_state(inference_state)` and discards every instance prompted so far.'
- **failure:** A later maintainer simplifies _add_box_prompts to the more obvious `bounding_boxes=` call, checks C26's authoritative list of six compensations, sees no entry protecting the corner-pair encoding, and ships it. Every object except the last prompted on a frame is silently discarded by reset_state; masks still come back and still pass the shape/count assertions, so Stage 5 gets confidently wrong per-instance masks. Separately, an auditor grepping masks.py for 'C26 compensation 5' expecting the cached_frame_outputs pre-seed lands on the box-prompt comment and concludes the pre-seed is unimplemented.

### [minor] pipeline/stage4_masks/masks.py:1382 — F2's sweep cannot recover a session whose close failed: discard precedes the close
- **claim:** `_close_session` does `self._owned_sessions.discard(session_id)` BEFORE `predictor.close_session(...)`, and then swallows RuntimeError/KeyError. So a session whose release genuinely fails is removed from the ownership set anyway, and `unload()`'s sweep (lines 1000-1002) — the whole point of F2 — can never retry it. The sweep therefore covers only the case where a caller forgot the id, not the case the comment at 995-996 names ('a window that raised past its close_video'). Worse, because the RuntimeError is swallowed inside `_close_session`, nothing propagates out of `close_video`, so Stage 3b's `n_sessions_unclosed` counter and its 'this session's device memory is leaked' cause string (track2d.py ~1678-1697) both stay silent for that session.
- **failure:** Stage 3b runs sam31_multiplex over many windows. On window k, `predictor.close_session` raises RuntimeError (CUDA release fault, or a state the predictor cannot tear down). `_close_session` has already discarded 'stage4-sam31-k' from `_owned_sessions` and swallows the error; `close_video` returns normally, so Stage 3b records zero unclosed sessions. The session's ~0.4 GB of frames and cached masks stay resident in `predictor._all_inference_states` for the rest of the process, and `unload()`'s sweep never touches it. Repeat over a scene and the run OOMs with a manifest that reports no leaked sessions.

### [minor] pipeline/stage4_masks/masks.py:1000 — F2's sweep is unguarded and runs ahead of the compensation-6 autocast teardown
- **claim:** The new `for session_id in sorted(self._owned_sessions): self._close_session(session_id)` is the first statement of `unload()` and is not wrapped in try/finally. `_close_session` only absorbs RuntimeError and KeyError; anything else propagates out of `unload()` before the bf16 `__exit__` walk, before `torch.set_autocast_enabled(..., False)` and before `torch.clear_autocast_cache()` — i.e. before the entire C26 compensation 6 the docstring immediately above it describes. Because `self._predictor = None` and `self._owned_sessions.clear()` also sit after the sweep, a retried `unload()` re-runs the same failing sweep, so the autocast state can never be restored by any number of calls.
- **failure:** API drift in `sam3` renames or repositions `close_session`'s parameter (the adapter guards against exactly this class of drift with `hasattr` assertions at lines 968-974). `predictor.close_session(session_id=...)` then raises TypeError, which `_close_session` does not catch. `unload()` aborts on the first owned session; the two bf16 autocast contexts the predictor entered in `__init__` stay entered and the ~1.28 GiB weight-cast cache is never dropped. Any caller that survives the failed unload (Stage 3b calls `adapter.unload()` in three places) then loads its next model inside a leaked bf16 autocast — the silent dtype change the docstring at 982-994 exists to prevent.

### [minor] pipeline/stage4_masks/masks.py:185 — MaskConfig.provenance states the pre-F3 routing rule and is echoed into every manifest
- **claim:** `MaskConfig.provenance['provider']` reads "C19: '' infers from model_id — facebook/sam3* -> sam3_tracker, mobile_sam* -> mobile_sam, anything else -> sam2_video". All three clauses are now false: after F3 removed the `facebook/sam3` prefix row, `facebook/sam3*` variants REFUSE rather than becoming sam3_tracker; `facebook/sam3.1*` routes to sam31_multiplex; and 'anything else -> sam2_video' is precisely the silent fallthrough C26 deleted. This dict is written verbatim into `manifest['config']` via `cfg.as_dict()` in run(), and from there into `Results/*/run_config.json` via save_run_results.py. The C26 ledger entry records the routing change but says nothing about F1's new checkpoint_path scoping, so the ledger cannot correct the manifest either.
- **failure:** An auditor opens `work_root/stage4_masks/run_manifest.json` (or a saved `Results/<run>/run_config.json`) to establish how the mask provider was chosen for a run — the document C26 says exists so 'a reader can see which model actually produced the masks'. The config block tells them an unrecognised model id would have silently become sam2_video and that `facebook/sam3.1` would have become sam3_tracker. Both readings are wrong for the code that produced that manifest, and the reader has no signal that the recorded rule is stale.

### [minor] pipeline/stage4_masks/masks.py:1063 — F1 guards on filename only while the pinned sha256 it computes is never compared
- **claim:** The refusal accepts ANY file whose basename is `sam3.1_multiplex.pt` and then stream-hashes it into `checkpoint_sha256` (line 942), but nothing ever compares that digest against the value C26 pins (0567debeec80ba4ac6369540c6c248025283cb3ff2b92827509e57e2b3541cb6). Per the repo's own note at smoke_sam31.py:637-639, `build_sam3_multiplex_video_predictor` 'prints missing/unexpected keys and discards them (model_builder.py:1223-1230)', so a wrongly-named file does not fail the build. The docstring's claim that the check prevents 'loading or hashing any other file' from being recorded 'as SAM 3.1's provenance' therefore holds only for the accidental $MOBILE_SAM_CHECKPOINT case it was written for, not for the general case.
- **failure:** An operator stages weights locally, e.g. `cp $MOBILE_SAM_CHECKPOINT /home/mt/Zami/weights/sam3.1_multiplex.pt` (or copies a truncated download under the right name), and runs the adapter with that `checkpoint_path`. The basename check passes, `_sha256_file` hashes 40 MB of MobileSAM, the builder discards every mismatched key and returns a predictor with untrained parameters, and Stage 4 writes 1600x900 masks that are geometrically well-formed noise. The manifest records a sha256 that looks authoritative and the run exits 0. Comparing the computed digest to the pinned constant before the build would catch it.

### [minor] scripts/compare_runs.py:26 — compare_runs prints no track2d data: the four A/B cells are indistinguishable
- **claim:** ROWS (lines 26-51) has no row sourced from `run_config.json` `track2d.*`, and the CONFIGURATION block (lines 109-123) reads only `proposal_2d` and `reid_embedding`. run_matrix_track2d.sh:103-104 invokes it as the matrix's summary artifact ("The comparison table is part of the artifact, not an afterthought"), but the only thing separating the four cells is Stage 3b — provider, refine flag, totals, elapsed_s and, crucially, `consumed_by_stage4`, all of which save_run_results.py records and none of which is printed.
- **failure:** Run the full matrix. All four columns print an identical CONFIGURATION block (same yolo11x checkpoint, same class map, same dinov3 re-ID) and the metric table has no 3b row, so the table cannot show whether 3b ran, whether refine was on, or whether the recovered boxes reached Stage 4. A cell whose `track2d.consumed_by_stage4` is false (stale-tree guard sent Stage 4 to stage3_proposals) prints as a full 3b arm — exactly the "a directory name is not evidence" failure save_run_results.py:8 was written to prevent.

### [minor] scripts/run_stages.sh:155 — TRACK2D_REVISION inherits MASK_REVISION independently of TRACK2D_MODEL_ID
- **claim:** `TRACK2D_MODEL_ID="${TRACK2D_MODEL_ID:-$MASK_MODEL_ID}"` and `TRACK2D_REVISION="${TRACK2D_REVISION:-$MASK_REVISION}"` are independent, so overriding the id alone silently keeps the other repo's commit sha. The same diff added the REID_PINNED table (lines 168-181) that maps id→sha and `exit 2` rather than mispair, but track2d got no equivalent guard, and docs/RUNNING.md:123 advertises exactly the unpaired form: "Stage 3b's `TRACK2D_MODEL_ID=facebook/sam3.1`".
- **failure:** Simulated `TRACK2D_MODEL_ID=facebook/sam3.1 scripts/run_stages.sh 3b`: `ARGV: [pipeline/stage3b_track2d/track2d.py] [--model-id] [facebook/sam3.1] [--revision] [3c879f39826c281e95690f02c7821c4de09afae7]` — facebook/sam3's sha sent to the sam3.1 repo. `hf_hub_download` 404s on the revision, the adapter raises ModelUnavailable, and Stage 3b exits 2 with a gated-repo hint that points the operator at an access problem they do not have. Same in reverse for `TRACK2D_REVISION=` alone.

### [minor] scripts/run_pilot.py:348 — run_pilot omits track2d from checkpoints and ignores TRACK2D_REFINE / TRACK2D_NO_SWEEPS
- **claim:** The aggregate manifest's `checkpoints` block (lines 345-349) lists proposal_2d, mask_2d and reid_embedding but not track2d, even though a run may include step 3b. Separately, run_stages.sh reads `TRACK2D_REFINE` and `TRACK2D_NO_SWEEPS` (lines 160-161, 524-525) to add `--refine-boxes` / `--no-sweep-detection`, and run_pilot.py's `stage_cmd("3b")` (lines 120-121) reads neither, so those two A/B arms are unreachable through the driver the plan calls "the contract-level driver".
- **failure:** `TRACK2D_REFINE=1 python -m scripts.run_pilot --steps 3 3b 4 5 6 7 8 9` runs the plain recovery arm, not the refine arm: the env var that works for run_stages.sh is silently ignored, and the operator attributes fill-arm numbers to the refine cell. Meanwhile the run's §1.9 identity block never names the tracker checkpoint that produced its proposals (recoverable only by parsing the per-step `cmd` array).

### [minor] scripts/export_cvat_coco.py:125 — track_id is a per-camera counter but exported unscoped: one id, six objects
- **claim:** Stage 3b restarts the track-id counter at 0 for every (scene, channel): in trial_3b scene-0061 there are 76 tracks but only 20 distinct id values, and ids 0-6 occur in all six cameras. masks.jsonl and the CVAT export both surface track_id as a bare integer attribute with no (scene, channel) qualifier, and export_cvat_coco.py's own docstring calls it 'track_id' without stating the scope. Nothing in the tree records that the id is only unique within a camera.
- **failure:** In trial_3b_stage4/scenes/scene-0061/masks.jsonl the very first keyframe row (023c4df2...) contains a CAM_BACK 'a truck' with track_id 2 and a CAM_FRONT 'a car' with track_id 2 -- two different objects. Per-channel id counts are CAM_FRONT 14 (0-13), CAM_FRONT_RIGHT 20 (0-19), CAM_BACK_RIGHT 17 (0-16), etc., summing to 76 tracks over a union of 20 ids; CAM_BACK/CAM_FRONT overlap on 9 ids, CAM_BACK_RIGHT/CAM_FRONT_RIGHT on 17. A reviewer in CVAT filtering attributes.track_id == 0, or any later consumer grouping masks.jsonl candidates by track_id to reconstruct a 2D tracklet, merges up to six unrelated objects into one identity.

### [minor] pipeline/stage4_masks/masks.py:1676 — Stage 4 asserts the 3b arrays are never short but never checks the lengths
- **claim:** The comment at :1673-1675 states the arrays are 'shorter than the box list on no row that this stage would accept', but the reads at :1699-1703 enforce nothing: an index past the end of box_sources silently yields 'yolo', past track_ids yields None, past n_propagated_hops yields 0. A row whose provenance arrays are shorter than boxes_xyxy_px is consumed without a word, and the resulting masks.jsonl asserts detector provenance for boxes no detector produced. Stage 3b is the only writer of these arrays and it is under active development; the trial rows happen to be aligned, so the invariant is currently true by luck rather than by check.
- **failure:** If a 3b run emits a row with 5 boxes but 4 entries in box_sources (a truncated append, an injection path that updates boxes before the parallel arrays, or a hand-patched row), stage 4 records the 5th candidate as box_source='yolo', track_id=null, n_propagated_hops=0. export_cvat_coco.py:124-126 then labels it attributes.source='yolo' in the review export, and a reviewer -- plus the C27 A/B, which attributes precision changes by source -- reads a recovered box as a detector box. No exception, no counter, no manifest field records that anything was defaulted. Contrast the aligned check that already exists for masks: result.assert_valid(n_boxes=len(boxes)) at :1664.

### [minor] scripts/eval_2d.py:97 — A/B compares a 0.40-thresholded detector against a 0.196-thresholded one
- **claim:** eval_2d applies no score floor: predictions_of drops only suppressed candidates and polygon twins, and kept.sort at :139 orders by score without gating it. Stage 3b injects recovered boxes whose decayed scores fall far below the detector's own acceptance threshold, so the 3b arm's prediction set contains boxes the baseline arm could not contain at any confidence. detect2d_metrics.json carries no per-source breakdown, so the resulting precision delta cannot be attributed between 'recovery found the wrong thing' and 'recovery operated at a lower threshold'.
- **failure:** In trial_3b scene-0061 the lowest YOLO score is 0.40039 (prompt.default_threshold is 0.40), but 57 of the 71 recovered boxes score below that, down to 0.19619. Those 57 reach cvat_export via masks.jsonl (67 of 71 recovered candidates are kept) and are counted one-for-one as predictions by eval_2d. When run_matrix_track2d.sh tabulates cell 1 (yolo11x_3b_base) against cell 2 (yolo11x_3b_fill) for C27 gate 2, cell 2's precision falls partly because 80% of its extra boxes sit below the confidence floor cell 1 operated at -- a threshold artifact read as a recovery-quality result, with nothing in the report able to distinguish the two.

### [minor] requirements-lock.txt:1 — setuptools==80.9.0, the pin that makes sam3 importable, is absent from the lock
- **claim:** requirements.txt:104-107 pins `setuptools==80.9.0` because 'sam3/model_builder.py imports pkg_resources at module level; setuptools 81+ removed it', and C26 repeats this as one of the four requirements.txt pins. `pip freeze` excludes setuptools by default, so the re-frozen lock does not contain it (a declared-vs-lock diff shows setuptools as the only real package declared but missing). requirements.txt rule 1 and the C19 amendment both name requirements-lock.txt as 'the exact resolution'. The installed env is correct (setuptools 80.9.0 verified), but the lock does not say so.
- **failure:** Someone rebuilds from the lock alone (which the header advertises as the verbatim regeneration target), gets whatever setuptools their pip ships — 81+ on any current pip, and this env demonstrably had 83.0.0 before the downgrade. `from sam3.model_builder import build_sam3_multiplex_video_predictor` raises ModuleNotFoundError: pkg_resources, Sam31MultiplexAdapter.load() converts it to ModelUnavailable, and Stage 4 refuses to start on the provider the lock was re-frozen to certify.

### [minor] docs/DECISIONS.md:512 — C19 amendment says the lock has '97 exact pins'; it has 101 entries
- **claim:** The C19 amendment added for C26 reads 'requirements-lock.txt was re-frozen (97 exact pins ...)'. The file carries 101 non-comment requirement lines: 97 use `==`, and four are direct references — clip, mobile_sam, pyzed and `sam3 @ git+...@8f0b7f4d4e7eda2ed606ebde6702c93359ad01da`. The three HF resolutions the amendment quotes (huggingface_hub 1.27.0 / tokenizers 0.22.2 / safetensors 0.8.0) are all correct and match the installed env; a full `pip freeze` diff against /home/mt/miniconda3/envs/ano_pipe is byte-identical to the lock.
- **failure:** A reader reconciling the count finds 101 entries and cannot tell whether four lines were added after the freeze or whether the ledger is counting a subset. The four excluded lines are exactly the ones whose pinning is contested — sam3's git sha (the C26 provenance anchor) and pyzed's dead /tmp path — so the count that omits them is the count that hides the problem.

### [minor] docs/RUNNING.md:111 — New SAM 3.1 row says '3.5 GiB ckpt'; the file is 3.26 GiB
- **claim:** The row's measured column says '3.5 GiB ckpt'. The checkpoint is 3,502,755,717 bytes = 3.50 GB decimal = 3.26 GiB. C26 (DECISIONS.md:872-874) says '3.50 GB' — correct. masks.py:859 and scripts/smoke_sam31.py:64 both say 'the checkpoint is ~3.3 GB' — which is the GiB figure carrying a GB label. So the same file is described as 3.5 GiB, 3.50 GB and ~3.3 GB across four files, and only one is right in its own units.
- **failure:** Anyone sizing the cache or a transfer from the RUNNING.md table (the document whose column header is 'Measured on this 4090') budgets 3.5 GiB = 3.76 GB, a 7.4% overstatement, and cannot reconcile it with the '~3.3 GB' in the two code comments — leaving no way to tell which of the three numbers was actually measured.

### [minor] docs/RUNNING.md:111 — Row quotes one mode and one direction but is labelled '(smoke, both modes)'
- **claim:** The row reads '~0.78 s / 32-box image, 0.105 s / video frame (smoke, both modes)'. C26 (DECISIONS.md:879-881) records '0.70/0.78 s per 32-box image (harness/adapter)' and 'video 0.105 s/frame forward and 0.139 s/frame reverse'. The row prints the adapter figure only and the forward figure only, then labels both '(smoke, both modes)'. Reverse propagation is 32% slower and disappears. The same truncation is baked into masks.py:1550-1555, `_PROVIDER_PROVENANCE['sam31_multiplex']` = '... smoke: 32/32 masks, 0.105 s/frame video, 7456 MiB peak ...', which is copied verbatim into `checkpoint.provenance` in every Stage 4 run_manifest.json for this provider.
- **failure:** Someone budgets Stage 3b's reverse pass (scripts/run_matrix_track2d.sh cell 4 runs both directions) from the 0.105 s/frame figure and under-estimates wall-clock by a third. Worse, the shortfall is then attributed to the run rather than to the quoted number, because the manifest's own provenance string carries the same single-direction figure as if it were the measurement.

### [minor] docs/RUNNING.md:188 — Stage 4 block lists only three --provider values, omitting sam31_multiplex
- **claim:** Line 188 reads: "The adapter is chosen from the model id (`--provider` overrides: `mobile_sam` / `sam2_video` / `sam3_tracker`)." masks.py:1531-1536 registers a fourth, `sam31_multiplex`, and C26 makes it the only adapter that can load facebook/sam3.1. The commented alternates in the code block just above (lines 182-184) likewise stop at SAM 2.1-L and MobileSAM. The new provider is named only 66 lines earlier, in prose at line 122.
- **failure:** A user working from the Stage 4 section copies the command block, adds `--model-id facebook/sam3.1`, and — seeing sam31_multiplex nowhere in the override list — passes `--provider sam3_tracker` to get 'the SAM 3 family'. run() raises UpstreamRefusal at masks.py:1774-1779 and the run dies at the CLI, with the correct value documented only in a paragraph the reader skipped.

### [minor] docs/RUNNING.md:93 — HF auth note still says the token is needed only for facebook/sam3
- **claim:** RUNNING.md:93 reads '# Hugging Face auth — needed once, only for the gated facebook/sam3 repo'. .env.example:25-28 was updated for C26 and now says 'two gated repos are now in play — facebook/sam3 ... and facebook/sam3.1 ... Both grants are held by the project account; the token is required on first download of either.' RUNNING.md, which C26 lists as recorded-in, kept the single-repo wording.
- **failure:** A second operator sets up the box, reads 'only for the gated facebook/sam3 repo', and does not confirm the sam3.1 grant on their account. Sam31MultiplexAdapter._resolve_checkpoint's hf_hub_download raises, surfacing as ModelUnavailable ('facebook/sam3.1 is a gated repo: HF_TOKEN must be set for the first download'), after they have already concluded from RUNNING.md that auth was a solved, one-repo problem.

### [minor] docs/comprehensive.md:21 — F6 flaw row still calls SAM 3.1 Object Multiplex a likely-hallucinated citation
- **claim:** This change-set amended only the §7.1 substitution row (comprehensive.md:370, appending '→ reinstated as the selectable sam31_multiplex provider by DECISIONS C26 ... F6's "unverifiable" condition no longer holds'). The F6 row in the flaw register at line 21 is untouched and still reads 'Unverifiable/likely-hallucinated citations ("SAM 3.1 Object Multiplex", Impact Scores, ...)'. The document now contradicts itself across 349 lines. Separately, C26's Recorded-in list (DECISIONS.md:925-930) does not mention docs/comprehensive.md at all, even though this is the file whose guard C26's 'Because' clause argues it satisfied.
- **failure:** A reviewer reading the flaw register top-down — the intended entry point, since it is the summary table — sees the pipeline's now-shipping mask provider listed as a likely hallucination and either escalates it or reverts the provider. Anyone trying to unwind C26 by following its Recorded-in list leaves the §7.1 amendment orphaned, because comprehensive.md is not on that list.

### [minor] docs/DECISIONS.md:926 — MaskVideoTracker booked to C26 in the ledger and to C27 in the code
- **claim:** C26's Recorded-in list says 'pipeline/common/model_interfaces.py (MaskVideoTracker protocol, no new registry role)'. The protocol's own docstring (model_interfaces.py:779) opens 'Stage 3b (C27). Box-prompted video tracking over an ordered frame window.' Every other C27 marker in masks.py agrees (masks.py:850, :904-906, :1252). C27 has no DECISIONS.md entry yet, so the protocol's only ledger record is under the wrong decision id.
- **failure:** When C27 is written it will claim model_interfaces.py as one of its recorded-in files, and the same 46 lines will be booked to two decisions. Anyone reverting C26 to unwind SAM 3.1 follows its Recorded-in list, removes the protocol, and breaks Stage 3b — which the code says owns it.

### [minor] docs/conformance.yaml:1713 — 5.5-r1: masks.py:857 now points inside the new SAM 3.1 adapter
- **claim:** Cited: 'masks.py:460,479-484: assertion exists but the wrong-rank branch raises IndexError ... escaping the except MaskContractError handler at :857'. NOW at those lines: :460 = `raise ModelUnavailable(` in MobileSamAdapter.load; :479-484 = MobileSamAdapter.unload's body and a blank line; :857 = `_BOX_POINT_LABELS: tuple[int, int] = (2, 3)`, a class constant of Sam31MultiplexAdapter. The real sites are now the shape assertion at masks.py:539-543 and the handlers at :823 (Transformers._propagate), :1237 (Sam31._propagate) and :2043 (main). The VIOLATES claim itself still holds: masks.py:539-542 and :740-743 both interpolate `masks_np.shape[2]` inside the raise that fires when `ndim != 3`, so a wrong-rank return raises IndexError, not MaskContractError.
- **failure:** A Phase 6 engineer takes the row's 'one-line fix' note, opens masks.py:460 and :479-484, finds adapter loading and teardown, then opens :857 and finds a SAM 3.1 point-label constant. The row reads as describing SAM 3.1 code. Either the fix is applied to the wrong adapter or the row is closed as stale — and the live IndexError at :539 and :740 survives.

### [minor] docs/conformance.yaml:1278 — 4-r3: masks.py:389 and :740-748 anchors wrong and the waiver is superseded
- **claim:** Cited: 'masks.py:389 supports_temporal=False; gap recorded per keyframe :740-748'. NOW :389 = `if ioa > ioa_threshold:` inside ioa_nms_across_cameras; :740-748 = TransformersSamAdapter's mask-shape MaskContractError. The real sites are MobileSamAdapter.supports_temporal→False at :444-447 and the per-keyframe propagation block at :1875-1881. Substance has moved too: the resolved default provider is facebook/sam3, whose supports_temporal is True (masks.py:608-611), C26 adds a third adapter that returns True (:904-907), and the per-keyframe text now reads 'provider supports video propagation (C19, best-effort); this run segmented per frame' (:1821-1827) — not the waiver the row records.
- **failure:** A reader checking whether the §4 propagation waiver is still the operative regime follows :389, lands in the NMS loop, and follows :740-748 into a shape check. Nothing at either anchor mentions propagation. The row's PLAUSIBLE status keeps asserting a MobileSAM-era waiver for a pipeline whose default and selectable mask providers both propagate.

### [minor] docs/conformance.yaml:1268 — 4-r2: masks.py:297-346 no longer spans ioa_nms_across_cameras
- **claim:** Cited: 'pipeline/stage4_masks/masks.py:297-346 ioa_nms_across_cameras, threshold 0.5'. NOW :297 = `return AngularBox(` inside angular_footprint (def at :262) and :346 = the `@property` above MaskCandidate.kept. ioa_nms_across_cameras is now at :351-400, and the 0.5 default lives at masks.py:151 (`ioa_threshold: float = 0.5`). The substance of the claim — real IoA-NMS in ego angular space at threshold 0.5 — still holds.
- **failure:** An auditor verifying M-2 opens masks.py:297-346, sees footprint construction and a dataclass property, and finds no NMS and no 0.5 anywhere in the cited range. The row is the sole CODE-SITE evidence for 'IoA-NMS across cameras is restored'; with the anchor unresolvable it is indistinguishable from an unsupported claim.

### [minor] docs/conformance.yaml:1723 — 5.5-r2: all four masks.py anchors now resolve to unrelated code
- **claim:** Cited: 'masks.py:297-346 (real, ego angular space) BUT :127 same_class_only=True and npz persists suppressed masks (:724 vs :750)'. NOW :127 = `_TWO_PI = 2.0 * math.pi`; :724 = `if len(boxes) == 0:` inside TransformersSamAdapter.segment; :750 = `window=window,` in a MaskResult constructor. The real sites: `same_class_only: bool = True` at masks.py:152, the npz write at :1859 (write_masks_npz), and the JSONL sidecar's candidate rows at :1882. The VIOLATES claim itself still holds — same_class_only still defaults True and the npz still stores every mask while the suppression verdict lives only in candidate_rows.
- **failure:** A reader chasing the cross-class duplicate defect opens :127 and gets a 2*pi constant, then :724/:750 and gets an empty-box guard and a keyword argument. Three of four anchors resolve to code with no bearing on the claim, so the row cannot be verified or closed from its own evidence.

### [minor] docs/conformance.yaml:1737 — 5.5-r3: the count-mismatch raise now has three sites, none at :474-478
- **claim:** Cited: 'masks.py:474-478 count-mismatch raise; assert_valid(n_boxes) :606'. NOW :474-478 = `self._model = sam` and the first lines of MobileSamAdapter.unload; :606 = a blank line inside TransformersSamAdapter. The one-mask-per-box raise is now at three sites — :533-537 (MobileSAM), :734-738 (Transformers), :1147-1151 (SAM 3.1, added by C26) — and `result.assert_valid(n_boxes=len(boxes), ...)` is at :1665. The claim holds and is in fact better supported than the row records, since C26 added a third enforcement site plus the obj_id-range guard at :1467-1476.
- **failure:** An engineer adding the misassignment test the row is waiting on writes a fixture against masks.py:474-478, which is now teardown code, and covers no adapter's assertion. The test passes vacuously and the row converts from PLAUSIBLE to CONFORMS on evidence that exercised nothing.

### [minor] docs/conformance.yaml:1747 — 5.5-r4: masks.py:425-433 now lands in __init__/roles, not segment()
- **claim:** Cited: 'masks.py:425-433; model_interfaces.py:754'. NOW masks.py:425 = `# in the run manifest.` (a comment in MobileSamAdapter.__init__) and :433 = `return (MASK_2D,)`. MobileSamAdapter.segment — the signature that accepts and ignores state/window — is now at :485-492 with the rationale at :496-499. model_interfaces.py:754 = a bare `"""`; `def segment(` in the Mask2D Protocol is now at :759. The claim still holds for all three adapters.
- **failure:** Someone verifying §7.1 fix 1 opens masks.py:425-433 and finds a manifest comment and a roles property — no `state`, no `window`. Both anchors in a two-anchor row miss, so the contract that exists cannot be demonstrated from the ledger.

### [minor] docs/conformance.yaml:1757 — 5.5-r5: masks.py:455 now an import; the row's load()-ordering note is false
- **claim:** Cited: 'masks.py:455 — no OutOfMemoryError handling anywhere in the stage'. NOW :455 = `from mobile_sam import SamPredictor, sam_model_registry` inside MobileSamAdapter.load. The ABSENT status still holds: `grep -c OutOfMemory pipeline/stage4_masks/masks.py` returns 0, and C26 added no OOM path. The row's note, however, asserts 'adapter.load() takes the GPU before upstream contract checks (:686-702)' — main() calls load_upstream (masks.py:2030) before run() (:2033), and adapter.load() sits at :1806 inside run(), after the upstream fingerprint checks. (This ordering was already correct at HEAD; the note predates it.)
- **failure:** A reader budgeting the Phase 5b OOM work follows :455 to an import statement and reads a note claiming the stage grabs the GPU before validating upstream. They plan a reordering fix that is already done, and the ordering claim — false and unmarked — propagates into whatever review consumes this row.

### [minor] docs/conformance.yaml:2711 — 10-r22: masks.py:675-681 now points at video-processor loading
- **claim:** Cited: 'proposals.py:900-906 / masks.py:675-681 hardcode ids inline; load_model_config never called by stages'. NOW masks.py:675-681 = the body of TransformersSamAdapter._ensure_video (`if self._is_sam3:` … `self._video_processor = processor_cls.from_pretrained(`). The inline CheckpointSpec construction is now at masks.py:1797-1803. The VIOLATES claim still holds — configs/ contains only coco_to_phrase_nuscenes.yaml, oiv7_to_phrase_nuscenes.yaml, paths.yaml and taxonomy_pilot_nuscenes.yaml, so models_pilot.yaml is still absent and load_model_config (model_interfaces.py:1148) is still uncalled.
- **failure:** C26 adds a fourth hardcoded checkpoint id (`facebook/sam3.1` as the --model-id default path plus the pinned revision) to exactly the site this row is supposed to anchor. An auditor opening :675-681 sees `from_pretrained` calls, concludes the row already describes the transformers loader, and never notices the config gap widened.

### [minor] docs/conformance.yaml:3066 — 14-r6: masks.py:740-748 'gap recorded' now points at a shape assertion
- **claim:** Cited: 'masks.py:740-748 gap recorded'. NOW :740-748 = TransformersSamAdapter's `if masks_np.ndim != 3 ...` MaskContractError and the following MaskResult fields. The capability-gap record is now the per-keyframe `"capability_gap": propagation_note` at masks.py:1875-1881 (text built at :1821-1827) and the manifest `capability_gaps` list at :1954-1960.
- **failure:** A claim-hygiene reviewer checking §14 opens :740-748 and finds an error message about mask dimensions with no propagation language. The row is the only evidence that the MobileSAM propagation loss is named as a capability gap; unresolvable, it cannot distinguish 'named honestly' from 'never named'.

### [minor] docs/conformance.yaml:936 — 1.9-r15: model_interfaces.py:1014 drifted +51, 46 of it from this change-set
- **claim:** Cited: 'scripts/measure_vram.py:830-834; model_interfaces.py:1014'. The MaskVideoTracker protocol inserted at model_interfaces.py:777-822 shifts every line past 776 by +46 (on top of +5 already accumulated). NOW :1014 = `    return instance` inside the registry's instance path. The intended anchor — the BudgetSpec docstring 'one legal value: an OOM or an over-ceiling measurement is a hard stop with ...' — is now at :1065.
- **failure:** Someone verifying the §1.9 OOM hard-stop contract before the Phase 5b gate opens model_interfaces.py:1014, finds a registry return statement, and has no evidence that the hard-stop semantics are declared anywhere — for the one row whose gate is 'hard-stop exercised at Phase 5b'.

### [minor] docs/conformance.yaml:2196 — 7-r3: all three model_interfaces.py anchors drifted +46
- **claim:** Cited: 'model_interfaces.py:894,876,956'. NOW :876 = `"""Return the missing/malformed members for `role`, most useful first."""`, :894 = an errors.append line, :956 = a RegistryError f-string. The intended anchors are now :927 (`def composite`), :945 (`role_tuple = ...`) and :1007 (`if singleton and name in _INSTANCES`). Every one of the three moved by exactly +46, the size of the MaskVideoTracker block added at :777-822.
- **failure:** An auditor confirming §7.1 fix 2 (one provider, multiple roles) opens all three cited lines and gets a docstring, an error message and an error message. Three-for-three misses make the row unverifiable — while C26/C27 in fact strengthen it, since Sam31MultiplexAdapter now satisfies two Protocols under one registration.

### [minor] docs/conformance.yaml:2226 — 7-r6: release() anchor drifted +51 and the VIOLATES text is now incomplete
- **claim:** Cited: 'model_interfaces.py:979-992 release() only drops refs; the assertion lives solely in measure_vram.py:251-269 which stages never invoke', with the note citing :966. NOW :979 = `def roles_of(name: str)` and :992 = `role = _check_role_name(role)`; `def release(name: str)` is at :1030 and `_factory_takes_role` at :1017. The 'solely in measure_vram.py' half is now wrong too: scripts/smoke_sam31.py's _release asserts `memory_allocated() < 50 MiB` and that autocast is off after teardown, and Sam31MultiplexAdapter.unload (masks.py:982-1043) is the first adapter to do real teardown — exiting both bf16 autocast contexts and clearing the ~1.28 GiB autocast cache.
- **failure:** Someone closing X-2 reads that no teardown assertion exists outside measure_vram.py, reimplements one, and duplicates the C26 smoke's check — or, following :979-992, edits roles_of/_check_role_name believing they are editing release().

### [minor] docs/conformance.yaml:2240 — 7-r7: model_interfaces.py:1120 and the note's :1014-1035 both drifted +51
- **claim:** Cited: 'configs/models_pilot.yaml — no such file; loader would take a bare FileNotFoundError (model_interfaces.py:1120)', note citing ':1014-1035'. NOW :1120 = `missing = [r for r in ROLES if r not in self.roles]`. load_model_config's unguarded `with open(path, ...)` is at :1171, and the BudgetSpec/on_exceed machinery the note points at is at :1060-1086. The ABSENT status still holds: configs/ contains no models_pilot.yaml.
- **failure:** Whoever finally writes configs/models_pilot.yaml (P0-8) checks the loader's failure mode at :1120, finds a role-completeness list comprehension, and ships the config without wrapping the bare FileNotFoundError the row exists to flag.

### [minor] docs/conformance.yaml:2176 — 7-r1: ReidEmbedding moved 51 lines; MaskVideoTracker sits directly above it
- **claim:** Cited: 'model_interfaces.py:693,716,742,773; ROLES :119'. NOW the four role Protocols are at :698 (EmbeddingOOD), :721 (Proposal2D), :747 (Mask2D) and :824 (ReidEmbedding); ROLES is at :121. The first three drifted 5-6 lines before this change-set; ReidEmbedding drifted a further 46 because MaskVideoTracker occupies :777-822, immediately above it. NOW :773 = a bare `"""` inside Mask2D.segment's docstring. The claim 'four role Protocols exist' still holds — MaskVideoTracker is deliberately a fifth Protocol outside ROLES (model_interfaces.py:781-783: 'NOT a registry role') — but the row says nothing about it.
- **failure:** A reviewer counting Protocols in model_interfaces.py after C26/C27 finds five `@runtime_checkable` Protocol classes against a row asserting four, with the fourth anchor landing in a docstring. Without the row recording the deliberate non-role status, the discrepancy reads as either an undeclared fifth role or a broken count.

### [minor] docs/conformance.yaml:1634 — 5.4-r2 records a VIOLATES that no longer exists: phrase_spans says CHAR
- **claim:** The row's evidence is 'model_interfaces.py:393 declaring phrase_spans as TOKEN spans while proposals.py:691 fills CHARACTER spans'. At the 56a65e7 baseline, :393 read '# (N,) [start, end) token span in the prompt'. It now reads (at :398) '# (N,) [start, end) CHAR span in the caption string', and :379 states 'phrase_spans carries the CHARACTER span [start, end) in the concatenated ...'. The declared contradiction is gone. (The fix landed in commit 9955298, not in this change-set; this change-set does not move model_interfaces.py lines <= 776, and its only proposals.py hunk is at :1476, below every anchor this row cites.)
- **failure:** 5.4-r2 is one of the 55 VIOLATES rows the rendered docs/CONFORMANCE.md tells readers to work through first ('Reading order: VIOLATES first'). Someone budgets Phase 6 work to reconcile span units that were reconciled at HEAD, and the real remaining half of the note — proposals.py falling back to '' when a phrase drops from the taxonomy — stays buried under a defect that is already fixed.

### [minor] docs/conformance.yaml:1838 — Eleven cluster.py-anchored rows drifted 5-119 lines from this change-set
- **claim:** cluster.py gained 131 lines with insertions starting at :162, so every conformance anchor at or past that line moved. Affected rows and their cited-vs-actual content: 3-r8 (:539 was `size_wlh_m: list`, now an angle comparison; :565-594), 3-r9 (:729/:775), 5.6-r6 (:1138-1149, was the manifest numpy_version block, now a totals dict), 5.7-r1 (:861/:706/:756/:405), 5.7-r2 (:305/:300/:325/:383-391), 5.7-r3 (:706/:722), 5.7-r4 (:565-604), 5.7-r5 (:654-676), 5.7-r6 (:936/:801), 10-r13 (:144 — the only cluster anchor still correct), 10-r14 (:383-391, was blank/docstring, now DBSCAN expansion code). Ten of the eleven now resolve to unrelated code.
- **failure:** Any reviewer working the §5.7 block — six rows, four of them the sole CODE-SITE evidence for L-shape fit, ghost filtering, eps provenance and the box round-trip — opens the cited lines and reads code from a different function. The rows cannot be confirmed, denied, or converted to CONFORMS from their own evidence.

### [minor] docs/conformance.yaml:1902 — Thirteen track.py-anchored rows drifted from this change-set's +111 lines
- **claim:** track.py gained 111 lines with insertions starting at :193. Verified per-anchor against HEAD: 4-r5 (:684-710 disambiguate_yaw → now `applied_near_cut_depth_m`), 5.8-r1 (:972/:837/:888-891), 5.8-r2 (:1205/:1276-1302/:1119-1142/:1145-1169), 5.8-r3 (:886-909/:1002/:1018), 5.8-r4 (:236/:244 moved; :184/:186 still correct), 5.8-r5 (:551-608/:1233-1234/:1240/:1257), 5.8-r6 (:150/:466-467/:1094-1096/:1219), 5.8-r7 (:1212 — the CloudCache relative-path defect, moved), 5.8-r8 (:1556 appearance-disabled silent re-run, moved), 5.8-r9 (:1194-1201/:1292-1293), 5.8-r10 (:718/:783-793), 10-r15 (:181-244), 10-r16 (:199-207). Only :98, :150, :181, :184, :186, :551 and :1302 still resolve to their original content.
- **failure:** 5.8-r7 is a VIOLATES row asserting a hard failure ('track.py:1212 uses det[points_path] verbatim ... CloudCache.get raises UpstreamRefusal on the first track birth'). :1212 is now `os.path.join(stage5_dir, det.get("points_path", ...`. A reader checking whether the bug is fixed reads a join at the cited line and closes the row, without establishing whether the actual call site — wherever it now is — was ever changed.

### [minor] docs/conformance.yaml:522 — Ten model_interfaces.py rows and all proposals.py rows stale from earlier commits
- **claim:** Precision on scope: this change-set touches model_interfaces.py only at :59 (an import) and :777-822 (the inserted protocol), so rows citing lines <= 776 did NOT move here — but they were already stale by 2-6 lines from commit 9955298: 1.5-r2 (:465, real site :470), 5.4-r1 (:275/:446 → :277/:451), 5.4-r3 (:306 → :308), 5.4-r6 (:289-295 → :291-297), 5.5-r4 and 7-r2 (:754 → :759), 7-r4 (:399/:351 → :404/:353), 7-r5 (:621-623 → :626-628). Likewise proposals.py's only hunk is at :1476, below every cited anchor — yet all of proposals.py's anchors are already wrong (e.g. 10-r22's :900-906 'builds CheckpointSpec inline' is now `class_names = ...` / `side_ok = ...`; 5.4-r6's :155 is blank).
- **failure:** Whoever repairs the anchors after C26 will diff against HEAD, fix only what this change-set moved, and leave these rows pointing at the wrong lines — locking in a second generation of drift in the same file. The list above is what a repair pass must also cover.

### [minor] scripts/check_conformance.py:96 — Validator never resolves evidence_ref anchors, so line drift is undetectable
- **claim:** Rule R2 is implemented as `if not str(r.get('evidence_ref','')).strip(): errors.append(...empty evidence_ref (R2))` — the only check on evidence_ref is non-emptiness. Nothing parses `path:line`, opens the file, or compares. That is why the masks.py anchors have survived two rounds of drift (they still match the 56a65e7 baseline, where masks.py was 883 lines; it was 1288 at HEAD and is 2078 now) while every validator run reported clean.
- **failure:** docs/CONFORMANCE.md is regenerated at any time — it was regenerated today at 14:19 — and re-publishes 205 CODE-SITE rows whose anchors nothing checked, under a header stamping a specific commit. Each regeneration re-certifies stale evidence as current, and the drift grows silently with every stage edit.

